/**
 * The custom configuration for your app. 
 * This is useful for one-off settings specific to your application-- 
 * things like the domain to use when sending emails, 
 * or 3rd party API keys for Stripe, Mailgun, Twitter, Facebook, etc.
 */

module.exports.custom = {
};